#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * @brief reads a pithy.txt file (should be present in the same directory as
 * the binary).
 * @returns 1 on errors; 0 otherwise.
 */
int reed();
