# Tiny C Projects

- yeah I know that this is literally the name of the book by Dan Gookins.  But, this repo contains programs I wrote while reading that book.  So, there you go.  I know, not the most creative of the names.
- I added a `Dockerfile` & a script to use the resulting *Docker image* while working on the code examples discussed in the book.
- I also added a `clang-format` config because, why not?!
- I didn't create a *CMake/Makefile* config because these programs are really simple most of the time.
